/**
*    created: 2025-04-13 21:33:20
**/
#include<bits/stdc++.h>
using namespace std;
#ifndef ONLINE_JUDGE
#include "../debug.cpp"
#else
#define debug(...)
#define debugArr(...)
#endif
#define int long long
#define show(x) cout << x << endl
#define sz(x) (int)(x.size())
/*
Brr Brrr Patapim is trying to learn of Tiramisù's secret passcode, which is a permutation∗
 of 2⋅𝑛
 elements. To help Patapim guess, Tiramisù gave him an 𝑛×𝑛
 grid 𝐺
, in which 𝐺𝑖,𝑗
 (or the element in the 𝑖
-th row and 𝑗
-th column of the grid) contains 𝑝𝑖+𝑗
, or the (𝑖+𝑗)
-th element in the permutation.

Given this grid, please help Patapim crack the forgotten code. It is guaranteed that the permutation exists, and it can be shown that the permutation can be determined uniquely.


*/
signed main(){
    ios_base::sync_with_stdio(false); cin.tie(NULL);
    auto solve_test = [&](int test_case){
        
    };
    int T = 1;
    cin >> T;
    for(int i = 0; i < T; ++i){
        solve_test(i);
    }
}